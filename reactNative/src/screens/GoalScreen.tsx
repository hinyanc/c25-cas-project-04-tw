import React from 'react';
import {SafeAreaView, Text, View} from 'react-native';

const GoalScreen = () => {
  return (
    <SafeAreaView>
      <View>
        <Text>Your BMI:</Text>
      </View>
    </SafeAreaView>
  );
};

export default GoalScreen;
